import { FETCHING_PEOPLE } from '../app.constants'

const initialState = {

}

export const fetchPeopleReducer = (state = initialState, action) => {
    switch (action.type) {
        case FETCHING_PEOPLE:

            break;

        default:
            break;
    }
}