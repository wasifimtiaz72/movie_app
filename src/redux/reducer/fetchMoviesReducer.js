
import { FETCHING_MOVIES } from '../app.constants'

const initialState = {

}


export const fetchMoviesReducer = (state = initialState, action) => {

    switch (action.type) {
        case FETCHING_MOVIES:

            break;

        default:
            break;
    }
}